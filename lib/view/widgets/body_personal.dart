import 'package:flutter/material.dart';

class personalBody extends StatelessWidget {
  const personalBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
